---
name: CI
# yamllint disable-line rule:truthy
on:
  push:

jobs:
  shellcheck:
    runs-on: ubuntu-latest
    steps:
      - name: Checkout repository
        uses: actions/checkout@v4
      - name: Install dependencies
        run: |
          sudo apt-get -y install --no-install-recommends \
            shellcheck
      - name: Run ShellCheck
        run: |
          shellcheck create-iso.sh
